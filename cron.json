{
    "jobs": [
        { "command": "0 0 1 * * npm run script:importSignedCommitsPercentage", "size": "S" },
        { "command": "0 0 1 * * npm run script:importStats", "size": "S" },
        { "command": "0 0 1 * * npm run script:importMetabaseVersions", "size": "S" }
    ]
}
